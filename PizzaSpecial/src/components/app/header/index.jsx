import React from 'react'
import {Helmet} from 'react-helmet'
const Header = () => {
    const menuList = ['Menu',];

    return (
        <div >
        <Helmet>
            TEST Header
    
        </Helmet>
        </div>
    );
};

export default Header;